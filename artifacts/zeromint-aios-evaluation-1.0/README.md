# ZeroMint AIOS engineering evaluation 1.0

Run `python verify_release.py` for arithmetic checks. Add `--live` to compare the recorded asset sizes and SHA-256 digests with the public GitHub release. Add `--torrent PATH` to verify an authorized local torrent copy. The verifier does not download, mount, boot, or install the 5.54 GiB ISO.
