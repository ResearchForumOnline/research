# OpenZero model and dataset methodology 1.0

Run `python verify_methodology.py` for public manifest checks. Authorized reviewers with the private curated snapshot may add `--curated-dir PATH` to recompute split counts, hashes, and exact final-user-prompt isolation. Dataset rows, teacher outputs, locked evaluations, and model weights are intentionally excluded.
