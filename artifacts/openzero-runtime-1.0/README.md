# OpenZero runtime 1.0 companion

Run `python verify_boundaries.py`. Passing confirms that eight declared action classes retain explicit locality, mutation, and confirmation fields and that unknown actions deny. It does not prove enforcement by an OpenZero deployment.
