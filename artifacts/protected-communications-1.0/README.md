# Protected communications 1.0 companion

This package records the cross-product layer model and dated test summary. Run `python verify_claims.py`. Passing verifies only that each layer retains a negative boundary and that protected, visibly standard, and denied outcomes remain distinct.
