# Bio-inspired recursive computation 1.0

Run `python verify_summary.py`. The package audits a frozen scalar formula through an independent mirror and supplies a deterministic bounded recurrence. Both are software experiments. They make no claim about genetics, DNA computing, wetware, consciousness, evolution, healing, or clinical effects.
